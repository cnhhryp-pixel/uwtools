// PDF Split browser module placeholder
// Next version will integrate pdf-lib for local page extraction.
(function(){
  window.splitPDF = function(file, pages){
    console.log('PDF split module ready', file, pages);
  };
})();
